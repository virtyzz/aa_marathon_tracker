"use client";import{useEffect,useState}from"react";import{signOut}from"next-auth/react";const days=["ЧТ","ПТ","СБ","ВС","ПН","ВТ","СР"];type A={id:string;name:string};type C={id:string;name:string;server?:string};type T={id:string;titleSnapshot:string;descriptionSnapshot:string;locationSnapshot?:string;xpSnapshot:number;allowedDaysSnapshot?:number[];maxCompletionsSnapshot:number};type D={week:{id:string;title:string;dateRange:string;weekTasks:T[]};weeks:{id:string;title:string;startsAt:string;endsAt:string;isActive:boolean}[];gameAccounts:A[];selectedAccountId:string;characters:C[];selectedCharacterId?:string;progresses:{weekTaskId:string;dayIndex:number}[];characterXp:Record<string,number>;stats:{xp:number;selectedXp:number};user:{name?:string;discordId?:string;isAdmin:boolean}};
export default function Home(){const[d,setD]=useState<D>(),[aid,setAid]=useState(""),[cid,setCid]=useState(""),[wid,setWid]=useState(""),[checks,setChecks]=useState(new Set<string>()),[show,setShow]=useState(false),[name,setName]=useState(""),[server,setServer]=useState(""),[msg,setMsg]=useState("");const load=async(w=wid,c=cid,a=aid)=>{const p=new URLSearchParams();if(w)p.set("weekId",w);if(c)p.set("characterId",c);if(a)p.set("accountId",a);const r=await fetch(`/api/dashboard?${p}`),x=await r.json();if(!r.ok){setMsg(x.error??"Ошибка загрузки");return}setD(x);setAid(x.selectedAccountId);setCid(x.selectedCharacterId??"");setWid(x.week.id);setChecks(new Set(x.progresses.map((v:{weekTaskId:string;dayIndex:number})=>`${v.weekTaskId}:${v.dayIndex}`)))};useEffect(()=>{void load()},[]);const add=async()=>{try{const r=await fetch("/api/characters",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({name,server:server||null,gameAccountId:aid})}),x=await r.json();if(!r.ok)throw Error(x.error);setShow(false);setName("");setServer("");await load(wid,x.id,aid)}catch(e){setMsg(e instanceof Error?e.message:"Не удалось создать персонажа")}};const toggle=async(t:T,i:number)=>{if(!cid){setMsg("Сначала добавьте и выберите персонажа");return}const k=`${t.id}:${i}`,next=new Set(checks),on=!next.has(k);on?next.add(k):next.delete(k);setChecks(next);const r=await fetch("/api/progress",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({characterId:cid,weekTaskId:t.id,dayIndex:i,completed:on})});if(!r.ok){setChecks(checks);setMsg((await r.json()).error??"Ошибка")}else await load()};return <main><header><div className="brand"><b>ArcheAge Marathon Tracker</b></div><nav><a className="nav active" href="/">Трекер</a>{d?.user.isAdmin&&<a className="nav" href="/admin">Управление</a>}<button className="nav" onClick={()=>signOut({callbackUrl:"/"})}>Выйти</button></nav><div className="profile"><b>{d?.user.name??"Discord"}</b><small>{d?.user.discordId}</small></div></header><section className="shell"><h1>{d?.week.title}</h1><p>{d?.week.dateRange}</p><section className="account-bar"><div><small>ИГРОВОЙ АККАУНТ</small><select value={aid} onChange={e=>void load(wid,"",e.target.value)}>{d?.gameAccounts.map(a=><option key={a.id} value={a.id}>{a.name}</option>)}</select></div><b>{d?.stats.xp??0}/100 XP</b></section><section className="characters"><b>Персонажи аккаунта:</b><button className="add" onClick={()=>setShow(true)}>+ Добавить персонажа</button>{d?.characters.map(c=><button className={c.id===cid?"char chosen":"char"} key={c.id} onClick={()=>void load(wid,c.id,aid)}><span>{c.name}<small>{c.server??"Сервер не указан"}</small></span><em>{d.characterXp[c.id]??0}/100</em></button>)}{!d?.characters.length&&<p>Добавьте персонажа, чтобы отмечать задания.</p>}</section><section className="week"><select value={wid} onChange={e=>void load(e.target.value,cid,aid)}>{d?.weeks.map(w=><option key={w.id} value={w.id}>{w.isActive?"● Активна · ":""}{w.title} · {new Date(w.startsAt).toLocaleDateString("ru-RU")} — {new Date(w.endsAt).toLocaleDateString("ru-RU")}</option>)}</select><span>Выбранный: {d?.stats.selectedXp??0}/100</span></section><section className="table-card"><div className="scroll"><table><thead><tr><th>Задание</th><th>XP</th><th>Описание / локация</th>{days.map(x=><th key={x}>{x}</th>)}<th>За нед.</th></tr></thead><tbody>{d?.week.weekTasks.map(t=>{const n=[...checks].filter(k=>k.startsWith(t.id+":")).length;return <tr key={t.id}><td className="quest">{t.titleSnapshot}</td><td>+{t.xpSnapshot}</td><td className="where">{t.descriptionSnapshot}{t.locationSnapshot&&<><br/>{t.locationSnapshot}</>}</td>{days.map((x,i)=>{const k=`${t.id}:${i}`;return <td className="check-cell" key={x}><button className={checks.has(k)?"box checked":"box"} disabled={!cid||!!t.allowedDaysSnapshot?.length&&!t.allowedDaysSnapshot.includes(i)||(!checks.has(k)&&n>=t.maxCompletionsSnapshot)} onClick={()=>void toggle(t,i)}>{checks.has(k)?"✓":""}</button></td>})}<td><span className="count">{n}/{t.maxCompletionsSnapshot}</span></td></tr>})}</tbody></table></div></section></section>{msg&&<div className="toast">{msg}</div>}{show&&<div className="modal-bg" onMouseDown={()=>setShow(false)}><div className="modal tracker-modal" role="dialog" aria-modal="true" onMouseDown={e=>e.stopPropagation()}><button className="close" onClick={()=>setShow(false)}>×</button><h2>Добавить персонажа</h2><input autoFocus placeholder="Имя персонажа" value={name} onChange={e=>setName(e.target.value)}/><input placeholder="Сервер (необязательно)" value={server} onChange={e=>setServer(e.target.value)}/><button className="create" disabled={!name.trim()} onClick={()=>void add()}>Добавить</button></div></div>}</main>}
