"use client";import{useEffect,useMemo,useState}from"react";import{signOut}from"next-auth/react";const days=["ЧТ","ПТ","СБ","ВС","ПН","ВТ","СР"];type A={id:string;name:string};type C={id:string;name:string;server?:string;note?:string};type T={id:string;titleSnapshot:string;descriptionSnapshot:string;locationSnapshot?:string;xpSnapshot:number;allowedDaysSnapshot?:number[];maxCompletionsSnapshot:number};type D={week:{id:string;title:string;startsAt:string;dateRange:string;weekTasks:T[]};weeks:{id:string;title:string;startsAt:string;endsAt:string;isActive:boolean}[];gameAccounts:A[];selectedAccountId:string;characters:C[];selectedCharacterId?:string;progresses:{weekTaskId:string;dayIndex:number}[];notes:{weekTaskId:string;text:string}[];characterXp:Record<string,number>;stats:{xp:number;selectedXp:number;todayXp:number;completions:number};user:{name?:string;discordId?:string;isAdmin:boolean}};
export default function Home(){const[d,setD]=useState<D>(),[aid,setAid]=useState(""),[cid,setCid]=useState(""),[wid,setWid]=useState(""),[checks,setChecks]=useState(new Set<string>()),[notes,setNotes]=useState<Record<string,string>>({}),[q,setQ]=useState(""),[msg,setMsg]=useState("");const load=async(w=wid,c=cid,a=aid)=>{const p=new URLSearchParams();if(w)p.set("weekId",w);if(c)p.set("characterId",c);if(a)p.set("accountId",a);const r=await fetch(`/api/dashboard?${p}`);if(!r.ok)return setMsg("Не удалось загрузить данные");const x:D=await r.json();setD(x);setAid(x.selectedAccountId);setCid(x.selectedCharacterId??"");setWid(x.week.id);setChecks(new Set(x.progresses.map(v=>`${v.weekTaskId}:${v.dayIndex}`)));setNotes(Object.fromEntries(x.notes.map(v=>[v.weekTaskId,v.text])))};useEffect(()=>{load()},[]);const api=async(url:string,m:string,b?:unknown)=>{const r=await fetch(url,{method:m,headers:{"Content-Type":"application/json"},body:b?JSON.stringify(b):undefined});if(!r.ok)throw new Error((await r.json()).error??"Ошибка")};const tasks=useMemo(()=>d?.week.weekTasks.filter(t=>`${t.titleSnapshot} ${t.descriptionSnapshot} ${t.locationSnapshot??""}`.toLowerCase().includes(q.toLowerCase()))??[],[d,q]);const toggle=async(t:T,i:number)=>{if(!cid)return setMsg("Выберите персонажа");const k=`${t.id}:${i}`,done=!checks.has(k),old=new Set(checks),next=new Set(checks);done?next.add(k):next.delete(k);setChecks(next);try{await api("/api/progress","POST",{characterId:cid,weekTaskId:t.id,dayIndex:i,completed:done});load()}catch(e){setChecks(old);setMsg(String(e))}};
const editAccount=async()=>{const name=prompt("Название аккаунта",d?.gameAccounts.find(a=>a.id===aid)?.name);if(name)try{await api(`/api/game-accounts/${aid}`,"PATCH",{name});load()}catch(e){setMsg(String(e))}};const deleteAccount=async()=>{if(confirm("Удалить аккаунт и его персонажей?"))try{await api(`/api/game-accounts/${aid}`,"DELETE");load("","","")}catch(e){setMsg(String(e))}};const editChar=async(c:C)=>{const name=prompt("Имя",c.name);if(!name)return;const server=prompt("Сервер",c.server??"");const note=prompt("Заметка",c.note??"");try{await api(`/api/characters/${c.id}`,"PATCH",{name,server:server||null,note:note||null});load()}catch(e){setMsg(String(e))}};
return <main><header><div className="brand"><b>ArcheAge Marathon Tracker</b></div><nav><a className="nav active" href="/">Трекер</a>{d?.user.isAdmin&&<a className="nav" href="/admin">Управление</a>}<button className="nav" onClick={()=>signOut({callbackUrl:"/"})}>Выйти</button></nav><div className="profile"><b>{d?.user.name??"Discord"}</b><small>{d?.user.discordId??""}</small></div></header><section className="shell"><h1>{d?.week.title??"Марафон"}</h1><p>{d?.week.dateRange}</p><section className="account-bar"><div><small>ИГРОВОЙ АККАУНТ</small><select value={aid} onChange={e=>load(wid,"",e.target.value)}>{d?.gameAccounts.map(a=><option key={a.id} value={a.id}>{a.name}</option>)}</select></div><b>{d?.stats.xp??0}/100 XP</b><button className="add" onClick={editAccount}>Изменить</button><button className="add" onClick={deleteAccount}>Удалить</button></section><section className="characters"><b>Персонажи аккаунта:</b>{d?.characters.map(c=><div className={c.id===cid?"char chosen":"char"} key={c.id}><button onClick={()=>load(wid,c.id,aid)}><span>{c.name}<small>{c.server??"Сервер не указан"}</small></span><em>{d.characterXp[c.id]??0}/100</em></button><button onClick={()=>editChar(c)}>✎</button></div>)}</section><section className="week"><select value={wid} onChange={e=>load(e.target.value,cid,aid)}>{d?.weeks.map(w=><option key={w.id} value={w.id}>{w.isActive?"● Активна · ":""}{w.title} · {new Date(w.startsAt).toLocaleDateString("ru-RU")} — {new Date(w.endsAt).toLocaleDateString("ru-RU")}</option>)}</select><span>Выбранный: {d?.stats.selectedXp??0}/100 · Сегодня +{d?.stats.todayXp??0}</span></section><section className="table-card"><div className="table-tools"><input placeholder="Поиск задания" value={q} onChange={e=>setQ(e.target.value)}/></div><div className="scroll"><table><thead><tr><th>Задание</th><th>XP</th><th>Описание / локация</th>{days.map(x=><th key={x}>{x}</th>)}<th>За нед.</th><th>Заметки</th></tr></thead><tbody>{tasks.map(t=>{const n=[...checks].filter(k=>k.startsWith(t.id)).length;return <tr key={t.id}><td>{t.titleSnapshot}</td><td>+{t.xpSnapshot}</td><td>{t.descriptionSnapshot}{t.locationSnapshot!==t.descriptionSnapshot&&<><br/>{t.locationSnapshot}</>}</td>{days.map((x,i)=>{const k=`${t.id}:${i}`;return <td className="check-cell" key={x}><button className={checks.has(k)?"box checked":"box"} disabled={!cid||!!t.allowedDaysSnapshot?.length&&!t.allowedDaysSnapshot.includes(i)||!checks.has(k)&&n>=t.maxCompletionsSnapshot} onClick={()=>toggle(t,i)}>{checks.has(k)?"✓":""}</button></td>})}<td><span className="count">{n}/{t.maxCompletionsSnapshot}</span></td><td><button className="note">{notes[t.id]??"Добавить"}</button></td></tr>})}</tbody></table></div></section></section>{msg&&<div className="toast">{msg}</div>}</main>}
